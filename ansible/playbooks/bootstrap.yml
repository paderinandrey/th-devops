---
- name: Bootstrap server for future ansible runs
  hosts: all
  become: yes
  remote_user: ubuntu
  gather_facts: False
  tasks:
    - name: Update apt packages
      raw: apt-get update
    - name: Install python
      raw: apt-get install python -y
